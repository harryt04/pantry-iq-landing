# CI cleanup investigation — 2026-08-10

## DEBUG REPORT

- **Symptom:** `npm run ci` stopped in the Playwright accessibility stage. The local web server could not bind under the restricted execution environment, and the run emitted `NO_COLOR`/`FORCE_COLOR` warnings. After that was isolated, the mobile body-copy gate found the marketing recommendation's observation-only paragraph at 12px. The retry test also wrote an expected provider exception to stderr.
- **Root cause:** The marketing card CSS omitted `.recommendation-card__observation-only` from its body-size rule. Playwright and its child processes inherited conflicting color flags from the invoking environment.
- **Fix:** Added the missing marketing selector; removed conflicting color variables at the Playwright command and web-server boundaries; silenced only the AI SDK's expected simulated provider exception in the retry test while retaining all retry/fallback assertions.
- **Evidence:** `npm run ci` completed with exit code 0. The final captured log reported 570 passing unit tests, 15 passing integration tests, 13 passing accessibility tests, 1 passing chart test, 2 passing E2E tests, a successful production build, and coverage completion. Its warning/error scan was clean.
- **Regression test:** Existing `tests/accessibility/landing.spec.ts` mobile body-copy test failed before the CSS fix and passes afterward. `src/server/chat/narration.test.ts` still asserts two provider attempts and degraded fallback.
- **Status:** DONE

## Follow-up: test output cleanup

- **Symptom:** Normal and coverage Vitest runs still printed `llm.query.completed` JSON records under `stdout` for the narration tests.
- **Root cause:** Three tests omitted the service logger, so they used the production logger and emitted successful mock-query telemetry.
- **Fix:** Added a test-only silent logger to those fixtures. Production callers still use the real logger.
- **Evidence:** Normal tests, coverage, and the complete `npm run ci` pass. The final CI log contains no `stdout`, `stderr`, `llm.query.completed`, warning, or error entries.
- **Status:** DONE
